import { CropperDirective } from './cropper.directive';

describe('CropperDirective', () => {
  it('should create an instance', () => {
    const directive = new CropperDirective();
    expect(directive).toBeTruthy();
  });
});
