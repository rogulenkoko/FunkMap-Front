import { DateNotificationPipe } from './date-notification.pipe';

describe('DateNotificationPipe', () => {
  it('create an instance', () => {
    const pipe = new DateNotificationPipe();
    expect(pipe).toBeTruthy();
  });
});
